<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark Mode app theme — inherits the main style so Material widgets
         automatically use night-mode color tokens.                        -->
    <style name="Base.Theme.BudgetBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Window background uses night-mode background color -->
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
